# 평단·손익 계산기 — MASTER PROMPT (빌드 명세)

> 이 앱을 처음부터 다시 만들거나 이어서 개선할 때 참고하는 마스터 명세.
> 새 기능/수정 시 이 문서를 함께 갱신한다.

## 목적
한국 주식·ETF의 분할매수/단타 상황에서, **평단·물타기·매도**를 수수료·세금까지 반영해
**순 실현손익**과 **손익분기 매도가**를 즉시 계산한다.

## 기술 구조
- `index.html` 하나에 HTML + CSS(inline) + JS(vanilla) 전부. **외부 의존성 없음(오프라인 동작).**
- `manifest.json` + `icon.png`로 PWA(홈 화면 설치).
- 호스팅: GitHub Pages (정적).

## 화면 구성
**1) 매수 (핑크 톤)**
- 보유 평단 / 보유 수량(0 가능)
- 분할 추가매수 영역(옐로우): 가격+수량 행을 **+버튼으로 여러 개** 추가/삭제, 기본가 = 보유 평단
- 출력: **최종 평균단가**(인디고 강조) / 총 수량 / 총 매수금

**2) 매도 (블루 톤)**
- 매도 예상가 / 매도 수량(기본=최종 총수량, "전량" 칩)
- 목표 수익률(%) · 목표 실현손익(원) → 매도가 자동 역산
- 수수료 계산 토글: 율(%) / 금액 직접
- 출력: 순 실현손익 + 순수익률(카드), 비용(수수료·거래세, 회색 1줄), 🎯 손익분기 매도가, 남은 보유분(일부 매도 시)

## 계산 로직
- 최종평단 = Σ(가격×수량) / Σ수량  (보유분 + 모든 추가매수)
- 율 모드: 순손익 = 매도금액×(1−c−t) − 매수원가×(1+c)   (c=수수료율, t=거래세율)
- 금액 모드: 순손익 = (매도금액 − 매수원가) − 수수료 − 제세금   (체결내역 직접 입력)
- 손익분기 매도가 = 평단 × (1+c) / (1−c−t)
- 목표수익률 r(%) → 매도가 = 평단 × (1 + c + r/100) / (1−c−t)
- 목표손익 P(원) → 매도가 = (P + 평단×수량×(1+c)) / (수량×(1−c−t))
- 남은 보유분 평가손익 = (매도예상가 − 평단) × 남은수량  (미실현)

## 호가 단위 (KRX, 가격대별 자동)
`<2,000:1 · <5,000:5 · <20,000:10 · <50,000:50 · <200,000:100 · <500,000:500 · ≥500,000:1,000`
가격 ±버튼은 현재가에 맞는 단위로 자동 조정.

## UX 규칙
- 가격창 **천단위 콤마**(계산 시 콤마 제거) · 숫자 길면 폰트 자동 축소.
- 매도예상가 기본값 = 손익분기 바로 위(호가 올림). 직접 수정하면 고정.
- 색: 매수=핑크 / 추가매수=옐로우 / 매도=블루 / 최종평단=인디고 강조. **다크모드 대응.**

## 참고 (세금/수수료)
- 수수료 = **증권사** (증권사·이벤트별 상이, 대략 0.014%).
- 거래세 = **정부(세금)**, 매도 시만. 국내주식 약 0.15~0.20%, **국내 ETF는 0%**.

## 공통 빌드 규칙 (모든 앱)
1. 앱 이름 + 아이콘(`icon.png`) + `manifest.json` 포함 → 홈 화면 앱화.
2. 버전업마다 `CHANGELOG.md`에 **[요청 · 변경 파일 · 변경 내용]** 추가.
3. 커밋 메시지에 버전 표기(예: `v1.1: …`).
4. 파일명은 `index.html` 고정(GitHub Pages 루트 자동 인식).
